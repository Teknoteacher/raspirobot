
#Sequence Robot

from rrb2 import *
#import time, random

rr = RRB2()

motor_speed = 1.0

rr.forward(3, motor_speed)
rr.right(1.5, motor_speed)
rr.forward(3, motor_speed)
rr.right(1.5, motor_speed)
rr.forward(3, motor_speed)
rr.right(1.5, motor_speed)
rr.forward(3, motor_speed)
rr.right(1.5, motor_speed)
